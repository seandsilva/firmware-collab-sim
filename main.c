void main() {
    // Sean: watchdog timer init
    // Marc: clock configuration
    // Marc: uart driver init - transmit ready
    // FIXED: sensor timeout bug - critical
    // WIP: new DMA config - not ready
}